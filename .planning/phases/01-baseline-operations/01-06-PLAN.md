---
phase: 01-baseline-operations
plan: 06
type: execute
wave: 4
depends_on:
  - "01-03"
files_modified:
  - .gitignore
  - tests/operations/config_templates_test.go
autonomous: true
gap_closure: true
requirements:
  - FOUND-02
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The exact development, staging, and production Compose environment files named by the operations runbooks are ignored by Git."
    - "Every --env-file path documented in the configuration and topology runbooks is discovered by an automated test and proven ignored."
    - "Committed *.env.example templates remain eligible for tracking while secret-bearing runtime copies cannot be staged accidentally."
  artifacts:
    - path: ".gitignore"
      provides: "Exact ignore policy for the three documented Compose runtime environment files"
      contains: "development.compose.env"
    - path: "tests/operations/config_templates_test.go"
      provides: "Runbook-to-ignore contract covering every documented --env-file occurrence"
      contains: "TestDocumentedComposeEnvFilesAreIgnored"
  key_links:
    - from: "tests/operations/config_templates_test.go"
      to: "docs/operations/configuration.md"
      via: "extracts each --env-file argument and checks it with git check-ignore --no-index"
      pattern: "configuration.md|--env-file|check-ignore"
    - from: "tests/operations/config_templates_test.go"
      to: "docs/operations/topology.md"
      via: "covers start and shutdown command paths rather than a hard-coded subset"
      pattern: "topology.md|--env-file"
    - from: ".gitignore"
      to: "deploy/config/*.compose.env"
      via: "explicit entries for the documented development, staging, and production runtime files"
      pattern: "development\\.compose\\.env|staging\\.compose\\.env|production\\.compose\\.env"
---

<objective>
Close the configuration-safety verification gap by making every Compose runtime environment file named by the operations runbooks untrackable and executable-policy tested.

Purpose: An operator following either runbook must not be able to stage the secret-bearing file supplied to `docker compose --env-file`.
Output: Exact Git ignore entries and a data-driven Go contract test tied to both operations documents.
</objective>

<execution_context>
@/Users/bryanjavier/.codex/gsd-core/workflows/execute-plan.md
@/Users/bryanjavier/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/WORKFLOW.md
@.planning/phases/01-baseline-operations/01-VERIFICATION.md
@.planning/phases/01-baseline-operations/01-01-SUMMARY.md
@.planning/phases/01-baseline-operations/01-03-SUMMARY.md
@.gitignore
@docs/operations/configuration.md
@docs/operations/topology.md
@tests/operations/config_templates_test.go

<interfaces>
Preserve `TestConfigurationTemplates` and its existing service-specific template checks.

Add `TestDocumentedComposeEnvFilesAreIgnored`, which reads both operations Markdown files from `repositoryRoot(t)`, extracts the whitespace-delimited path immediately following every `--env-file` token, and invokes `git check-ignore --no-index --quiet -- {path}` with the repository root as the command directory.

The canonical unique path set is exactly:
- `deploy/config/development.compose.env`
- `deploy/config/staging.compose.env`
- `deploy/config/production.compose.env`
</interfaces>
</context>

<source_audit>

| Source | ID | Phase 1 item | Plan coverage | Status |
|--------|----|--------------|---------------|--------|
| GOAL | — | Build and run safely without secrets or public Rust access | 01-01 through 01-07; this plan closes the remaining configuration blocker | COVERED |
| REQ | FOUND-01 | Supported reproducible baseline | 01-02 | COVERED |
| REQ | FOUND-02 | Ignored configuration for development, staging, and production | 01-01, 01-03, 01-04, 01-06 | COVERED |
| REQ | FOUND-03 | Go-only public topology | 01-01, 01-03 | COVERED |
| REQ | FOUND-04 | Sensitive data excluded from outputs | 01-04, 01-05, 01-07 | COVERED |
| RESEARCH | — | Exact toolchain and immutable lockfile baseline | 01-02 | COVERED |
| RESEARCH | — | Ignored runtime files with committed safe templates | 01-01, 01-03, 01-06 | COVERED |
| RESEARCH | — | Private Rust network and Go-only exposure | 01-01, 01-03 | COVERED |
| RESEARCH | — | Producer-side Go and Rust redaction with sentinel tests | 01-04, 01-05, 01-07 | COVERED |
| CONTEXT | — | No Phase 1 CONTEXT.md exists | — | N/A |
| VERIFICATION | Gap 1 | Documented Compose env files are trackable and untested | 01-06 | COVERED |

</source_audit>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make the three documented Compose runtime paths untrackable</name>
  <files>.gitignore, tests/operations/config_templates_test.go</files>
  <behavior>
    - Test 1: Git reports `deploy/config/development.compose.env` as ignored.
    - Test 2: Git reports `deploy/config/staging.compose.env` as ignored.
    - Test 3: Git reports `deploy/config/production.compose.env` as ignored.
    - Test 4: the three committed service templates remain eligible for tracking.
  </behavior>
  <action>Start by extending the current configuration-template test table with the three Compose runtime paths named in the runbooks; run the focused test and observe failure against the current ignore policy. Add explicit root-anchored `.gitignore` entries for `deploy/config/development.compose.env`, `deploy/config/staging.compose.env`, and `deploy/config/production.compose.env`, preserving the existing service-specific development/staging/production rules and the tracking eligibility of every `*.env.example`. Use `git check-ignore --no-index --quiet --` so the contract proves ignore behavior even when the runtime files do not exist. Do not create any secret-bearing environment file.</action>
  <verify>
    <automated>docker run --rm -v "$PWD:/workspace" -w /workspace golang:1.26.5-alpine sh -ceu 'apk add --no-cache git >/dev/null; go test ./tests/operations -run TestConfigurationTemplates -count=1'</automated>
  </verify>
  <done>All three exact Compose runtime paths are ignored, all safe examples remain trackable, and the focused configuration contract passes for FOUND-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind every runbook --env-file argument to the ignore contract</name>
  <files>tests/operations/config_templates_test.go</files>
  <behavior>
    - Test 1: every `--env-file` occurrence in `docs/operations/configuration.md` is extracted and reports ignored.
    - Test 2: every start and shutdown `--env-file` occurrence in `docs/operations/topology.md` is extracted and reports ignored.
    - Test 3: the unique extracted set is exactly the development, staging, and production Compose runtime paths, so a missing environment or an undocumented filename drift fails visibly.
    - Test 4: zero extracted arguments fails the test before any absence or ignore assertion can pass.
  </behavior>
  <action>Write `TestDocumentedComposeEnvFilesAreIgnored` and a small dependency-free helper in `tests/operations/config_templates_test.go`. Read both runbooks, tokenize their shell command text, collect every argument immediately following `--env-file`, and fail when either document contributes no paths. For every occurrence, reject absolute paths and paths outside `deploy/config`, then run the existing Git ignore probe. Deduplicate the results and compare them with the exact three-path canonical set so development, staging, and production are all represented while repeated start/config/down commands remain individually checked. Keep the current runbook filenames unchanged because they already express the approved operator workflow; the defect is the missing ignore/test wiring.</action>
  <verify>
    <automated>docker run --rm -v "$PWD:/workspace" -w /workspace golang:1.26.5-alpine sh -ceu 'apk add --no-cache git >/dev/null; go test ./tests/operations -run "TestConfigurationTemplates|TestDocumentedComposeEnvFilesAreIgnored" -count=1'</automated>
  </verify>
  <done>Both runbooks are mechanically tied to the ignore policy, every documented `--env-file` occurrence is checked, and path drift cannot silently reintroduce a trackable secret file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator secret input -> `deploy/config/*.compose.env` | Real credentials and image references enter local runtime files named by the runbooks. |
| operations documentation -> shell invocation | Documented paths determine which secret-bearing files operators create and pass to Compose. |
| repository ignore rules -> Git index | A missing or mismatched pattern permits runtime secrets to cross into source control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06-01 | Information Disclosure | `.gitignore` and Compose runtime files | high | mitigate | Add exact root-anchored rules for all three documented files and prove each with `git check-ignore --no-index`. |
| T-01-06-02 | Tampering | operations runbook paths | medium | mitigate | Extract every documented argument dynamically, reject paths outside `deploy/config`, and require the canonical three-environment set. |
| T-01-06-03 | Repudiation | configuration safety test | low | accept | The test records the offending document and path on failure; more detailed audit history is unnecessary for a repository-local policy check. |
| T-01-SC | Tampering | existing Go test dependency graph | high | mitigate | Add no package, retain `go.mod`, and run the contract with the pinned Go 1.26.5 image. |
</threat_model>

<verification>
- Run `docker run --rm -v "$PWD:/workspace" -w /workspace golang:1.26.5-alpine sh -ceu 'apk add --no-cache git >/dev/null; go test ./tests/operations -run "TestConfigurationTemplates|TestDocumentedComposeEnvFilesAreIgnored" -count=1'`.
- Run `git check-ignore --no-index -v -- deploy/config/development.compose.env deploy/config/staging.compose.env deploy/config/production.compose.env`; all three paths must match explicit rules.
- Confirm `git check-ignore --no-index --quiet -- deploy/config/go.env.example` returns non-zero, and repeat for the Rust and Supabase examples.
</verification>

<success_criteria>
- The exact Compose environment files used by both operations guides are ignored across development, staging, and production.
- Automated coverage follows every documented `--env-file` occurrence rather than a disconnected hard-coded subset.
- No runtime secret file, dependency, topology change, or later-phase product behavior is introduced.
</success_criteria>

<output>
Create `.planning/phases/01-baseline-operations/01-06-SUMMARY.md` when done.
</output>
